import * as React from 'react';

export type DropDownProps = {
    button: React.ReactElement<{ onClick?: any }>,
    children
}

export class DropDown extends React.PureComponent<DropDownProps> {
    render() {

    }
}